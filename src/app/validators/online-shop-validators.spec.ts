import { OnlineShopValidators } from './online-shop-validators';

describe('OnlineShopValidators', () => {
  it('should create an instance', () => {
    expect(new OnlineShopValidators()).toBeTruthy();
  });
});
